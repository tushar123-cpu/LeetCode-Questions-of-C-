class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int newLength = 0; 
        for (int element : nums) {
            if (element != val) {
                nums[newLength++] = element;
            }
        }
        return newLength;
    }
};
